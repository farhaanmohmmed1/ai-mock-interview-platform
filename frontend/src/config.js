// API Configuration
// In development, use empty string to leverage Vite proxy (configured in vite.config.js)
// In production (Vercel), also use relative URLs

export const API_URL = '';

export default API_URL;
